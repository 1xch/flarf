Filters Flask and extracts some information for your use.

Simplest example: 

    from flask import Flask
    from flask.ext.flarf import Flarf

    filter1 = FlarfFilter(filter_tag='filter1',
                          filter_precedence=100,
                          filter_params=['path',],
                          filter_on=['includeme'])

   app = Flask(__name__)
   Flarf(app, filters=[filter1])

   @route("/")
   def index():
       return render_template("index.html", filter1=g.filter1)
